/** App branding (override name via `NEXT_PUBLIC_APP_NAME` in `.env.local`). */
export const APP_NAME =
  process.env.NEXT_PUBLIC_APP_NAME?.trim() || "FoodieLens";

export const APP_TAGLINE = "Discover dining through the lens of flavor.";
